<?xml version="1.0" encoding="utf-8"?>
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_upload_registrations"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.dbs.jmulhall.projectsafehavenapi25.UploadRegistrations">

        <TextView
            android:text="Upload Registrations"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/lblUploadRegistrations"
            android:textSize="24sp"
            android:textStyle="normal|bold"
            android:textAlignment="center"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true" />

        <ListView
            android:layout_width="match_parent"
            android:layout_centerHorizontal="true"
            android:layout_below="@+id/lblUploadRegistrations"
            android:id="@+id/lstViewUploads"
            android:layout_height="500sp" />

    <Button
        android:text="Home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnHome"
        android:textSize="14sp"
        android:textStyle="normal|bold"
        android:onClick="toToHomeFromUploadReg"
        android:layout_alignParentBottom="true"
        android:layout_alignEnd="@+id/lblUploadRegistrations" />

    <Button
        android:text="Upload"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnUpload"
        android:textStyle="normal|bold"
        android:onClick="uploadRegistration"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@+id/btnHome"
        android:layout_marginEnd="37dp" />

</RelativeLayout>